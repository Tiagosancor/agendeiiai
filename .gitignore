# Segredos e configuração local — nunca commitar (seção 8.5.2).
.env
.env.local
*.env.local

# Documentação interna — fica só local, não sobe para o GitHub (pedido do usuário).
# decisoes.md/deploy.md/migracao.md continuam sendo escritos aqui normalmente; só não
# entram no repositório remoto. CLAUDE.md pelo mesmo motivo.
/docs/
/CLAUDE.md

# .NET
backend/**/bin/
backend/**/obj/
backend/**/*.user
backend/.vs/

# Node / Next.js (o frontend também tem seu próprio .gitignore mais detalhado)
frontend/node_modules/
frontend/.next/

# Dados locais (volumes montados por bind mount, se algum dia usarmos)
**/dados-locais/

# Backups locais gerados por scripts/backup.sh — nunca commitar (dado pessoal cifrado, mas ainda assim).
/backups/

# IDE / SO
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
Thumbs.db
.DS_Store
